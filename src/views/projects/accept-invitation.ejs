<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Aceptar Invitación - <%= project.nombre %></title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0;
        }
        .invitation-card {
            background: white;
            border-radius: 20px;
            padding: 40px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
            max-width: 500px;
            text-align: center;
        }
        .project-icon {
            font-size: 48px;
            color: #667eea;
            margin-bottom: 20px;
        }
        .btn {
            padding: 12px 30px;
            border: none;
            border-radius: 25px;
            font-weight: 600;
            text-decoration: none;
            display: inline-block;
            margin: 10px;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        .btn-primary {
            background: linear-gradient(45deg, #667eea, #764ba2);
            color: white;
        }
        .btn-secondary {
            background: #6c757d;
            color: white;
        }
    </style>
</head>
<body>
    <div class="invitation-card">
        <div class="project-icon">
            <i class="fas fa-envelope-open"></i>
        </div>
        <h1>Invitación al Proyecto</h1>
        <h2><%= project.nombre %></h2>
        <p><%= project.descripcion || 'Sin descripción disponible' %></p>
        
        <% if (invitation.mensaje) { %>
        <div style="background: #f8f9fa; padding: 15px; border-radius: 10px; margin: 20px 0;">
            <strong>Mensaje:</strong> <%= invitation.mensaje %>
        </div>
        <% } %>
        
        <div style="margin-top: 30px;">
            <form action="/projects/invitations/accept/<%= codigo %>" method="POST" style="display: inline;">
                <button type="submit" class="btn btn-primary">
                    <i class="fas fa-check"></i> Aceptar Invitación
                </button>
            </form>
            
            <form action="/projects/invitations/reject/<%= codigo %>" method="POST" style="display: inline;">
                <button type="submit" class="btn btn-secondary">
                    <i class="fas fa-times"></i> Rechazar
                </button>
            </form>
        </div>
        
        <p style="margin-top: 20px; color: #666; font-size: 14px;">
            ¿No tienes cuenta? <a href="/auth/register">Regístrate aquí</a>
        </p>
    </div>
</body>
</html>