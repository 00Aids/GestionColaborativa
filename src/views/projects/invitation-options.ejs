<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Únete al Proyecto - <%= project.titulo %></title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0;
            padding: 20px;
        }
        .invitation-container {
            background: white;
            border-radius: 20px;
            padding: 40px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
            max-width: 800px;
            width: 100%;
        }
        .project-header {
            text-align: center;
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 2px solid #f1f5f9;
        }
        .project-icon {
            font-size: 48px;
            color: #667eea;
            margin-bottom: 15px;
        }
        .options-container {
            display: flex;
            gap: 30px;
            margin-top: 30px;
        }
        .option-card {
            flex: 1;
            background: #f8fafc;
            border-radius: 15px;
            padding: 30px;
            border: 2px solid transparent;
            transition: all 0.3s ease;
            cursor: pointer;
        }
        .option-card:hover {
            border-color: #667eea;
            transform: translateY(-5px);
            box-shadow: 0 10px 30px rgba(102, 126, 234, 0.1);
        }
        .option-card.active {
            border-color: #667eea;
            background: #f0f4ff;
        }
        .option-icon {
            font-size: 36px;
            color: #667eea;
            margin-bottom: 15px;
            text-align: center;
        }
        .form-container {
            display: none;
            margin-top: 30px;
            padding: 30px;
            background: #f8fafc;
            border-radius: 15px;
            border: 2px solid #667eea;
        }
        .form-container.active {
            display: block;
            animation: fadeIn 0.3s ease;
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .btn-custom {
            padding: 12px 30px;
            border: none;
            border-radius: 25px;
            font-weight: 600;
            background: linear-gradient(45deg, #667eea, #764ba2);
            color: white;
            cursor: pointer;
            transition: all 0.3s ease;
            width: 100%;
        }
        .btn-custom:hover {
            background: linear-gradient(45deg, #5a67d8, #6b46c1);
            transform: translateY(-2px);
        }
        .form-group {
            margin-bottom: 20px;
        }
        .form-control {
            border-radius: 10px;
            border: 2px solid #e2e8f0;
            padding: 12px 15px;
            transition: border-color 0.3s ease;
        }
        .form-control:focus {
            border-color: #667eea;
            box-shadow: 0 0 0 0.2rem rgba(102, 126, 234, 0.25);
        }
        .project-info {
            background: #e0f2fe;
            padding: 20px;
            border-radius: 15px;
            margin-bottom: 20px;
            border-left: 4px solid #0284c7;
        }
        .alert {
            border-radius: 10px;
            margin-bottom: 20px;
        }
        @media (max-width: 768px) {
            .options-container {
                flex-direction: column;
                gap: 20px;
            }
            .invitation-container {
                padding: 20px;
            }
        }
    </style>
</head>
<body>
    <div class="invitation-container">
        <!-- Header del proyecto -->
        <div class="project-header">
            <div class="project-icon">
                <i class="fas fa-graduation-cap"></i>
            </div>
            <h1 class="mb-3">¡Has sido invitado!</h1>
            <h2 class="text-primary mb-3"><%= project.titulo %></h2>
            
            <div class="project-info">
                <h6><i class="fas fa-info-circle"></i> Detalles del Proyecto</h6>
                <p class="mb-2"><strong>Nombre:</strong> <%= project.titulo %></p>
                <% if (project.descripcion) { %>
                <p class="mb-2"><strong>Descripción:</strong> <%= project.descripcion %></p>
                <% } %>
                <p class="mb-0"><strong>Invitado por:</strong> <%= invitation.invitado_por_nombre || 'Administrador' %></p>
            </div>
            
            <% if (invitation.mensaje) { %>
            <div class="alert alert-info">
                <h6><i class="fas fa-comment"></i> Mensaje personal:</h6>
                <p class="mb-0"><%= invitation.mensaje %></p>
            </div>
            <% } %>
        </div>

        <!-- Mostrar mensajes flash -->
        <% if (typeof messages !== 'undefined') { %>
            <% if (messages.error) { %>
                <div class="alert alert-danger">
                    <i class="fas fa-exclamation-triangle"></i> <%= messages.error %>
                </div>
            <% } %>
            <% if (messages.success) { %>
                <div class="alert alert-success">
                    <i class="fas fa-check-circle"></i> <%= messages.success %>
                </div>
            <% } %>
        <% } %>

        <div class="text-center mb-4">
            <h4>Para unirte al proyecto, elige una opción:</h4>
            <p class="text-muted">Te unirás automáticamente como <strong>Estudiante</strong> y el administrador podrá cambiar tu rol después.</p>
        </div>

        <!-- Opciones de registro/login -->
        <div class="options-container">
            <div class="option-card" onclick="showForm('register')">
                <div class="option-icon">
                    <i class="fas fa-user-plus"></i>
                </div>
                <h5 class="text-center mb-3">Crear Nueva Cuenta</h5>
                <p class="text-center text-muted">No tengo una cuenta en el sistema</p>
                <div class="text-center">
                    <small class="text-success"><i class="fas fa-check"></i> Registro rápido y fácil</small>
                </div>
            </div>

            <div class="option-card" onclick="showForm('login')">
                <div class="option-icon">
                    <i class="fas fa-sign-in-alt"></i>
                </div>
                <h5 class="text-center mb-3">Iniciar Sesión</h5>
                <p class="text-center text-muted">Ya tengo una cuenta</p>
                <div class="text-center">
                    <small class="text-primary"><i class="fas fa-user-check"></i> Acceso inmediato</small>
                </div>
            </div>
        </div>

        <!-- Formulario de registro -->
        <div id="registerForm" class="form-container">
            <h5 class="mb-4"><i class="fas fa-user-plus"></i> Crear Nueva Cuenta</h5>
            <form action="/projects/invitations/register/<%= codigo %>" method="POST">
                <div class="row">
                    <div class="col-md-6">
                        <div class="form-group">
                            <label for="nombres" class="form-label">Nombres *</label>
                            <input type="text" class="form-control" id="nombres" name="nombres" required>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="form-group">
                            <label for="apellidos" class="form-label">Apellidos *</label>
                            <input type="text" class="form-control" id="apellidos" name="apellidos" required>
                        </div>
                    </div>
                </div>
                
                <div class="form-group">
                    <label for="email" class="form-label">Email *</label>
                    <input type="email" class="form-control" id="email" name="email" required>
                </div>
                
                <div class="row">
                    <div class="col-md-6">
                        <div class="form-group">
                            <label for="password" class="form-label">Contraseña *</label>
                            <input type="password" class="form-control" id="password" name="password" required minlength="6">
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="form-group">
                            <label for="confirm_password" class="form-label">Confirmar Contraseña *</label>
                            <input type="password" class="form-control" id="confirm_password" name="confirm_password" required minlength="6">
                        </div>
                    </div>
                </div>
                
                <div class="alert alert-info">
                    <small><i class="fas fa-info-circle"></i> Tu cuenta se creará con rol de <strong>Estudiante</strong> para este proyecto.</small>
                </div>
                
                <button type="submit" class="btn-custom">
                    <i class="fas fa-user-plus"></i> Crear Cuenta y Unirse al Proyecto
                </button>
            </form>
        </div>

        <!-- Formulario de login -->
        <div id="loginForm" class="form-container">
            <h5 class="mb-4"><i class="fas fa-sign-in-alt"></i> Iniciar Sesión</h5>
            <form action="/projects/invitations/login/<%= codigo %>" method="POST">
                <div class="form-group">
                    <label for="login_email" class="form-label">Email *</label>
                    <input type="email" class="form-control" id="login_email" name="email" required>
                </div>
                
                <div class="form-group">
                    <label for="login_password" class="form-label">Contraseña *</label>
                    <input type="password" class="form-control" id="login_password" name="password" required>
                </div>
                
                <div class="alert alert-info">
                    <small><i class="fas fa-info-circle"></i> Te unirás al proyecto con rol de <strong>Estudiante</strong>.</small>
                </div>
                
                <button type="submit" class="btn-custom">
                    <i class="fas fa-sign-in-alt"></i> Iniciar Sesión y Unirse al Proyecto
                </button>
                
                <div class="text-center mt-3">
                    <small class="text-muted">
                        ¿No tienes cuenta? 
                        <a href="#" onclick="showForm('register')" class="text-primary">Crear una nueva</a>
                    </small>
                </div>
            </form>
        </div>

        <div class="text-center mt-4">
            <small class="text-muted">
                <i class="fas fa-shield-alt"></i> 
                Esta invitación es segura y fue enviada por el Sistema de Gestión Colaborativa Académica.
            </small>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        function showForm(type) {
            // Remover clases activas
            document.querySelectorAll('.option-card').forEach(card => {
                card.classList.remove('active');
            });
            document.querySelectorAll('.form-container').forEach(form => {
                form.classList.remove('active');
            });
            
            // Activar la opción seleccionada
            event.currentTarget.classList.add('active');
            document.getElementById(type + 'Form').classList.add('active');
            
            // Scroll suave al formulario
            document.getElementById(type + 'Form').scrollIntoView({
                behavior: 'smooth',
                block: 'nearest'
            });
        }

        // Validación de contraseñas en tiempo real
        document.getElementById('confirm_password')?.addEventListener('input', function() {
            const password = document.getElementById('password').value;
            const confirmPassword = this.value;
            
            if (password !== confirmPassword) {
                this.setCustomValidity('Las contraseñas no coinciden');
            } else {
                this.setCustomValidity('');
            }
        });
    </script>
</body>
</html>