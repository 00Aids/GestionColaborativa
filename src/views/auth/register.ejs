<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'Registro' %></title>
    <%- include('../partials/head') %>
</head>
<body class="min-vh-100 d-flex align-items-center">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-10 col-xl-8">
                <div class="card shadow-soft overflow-hidden">
                    <div class="row g-0">
                        <!-- Panel académico -->
                        <div class="col-md-5 gradient-uts p-4 p-md-5 d-flex flex-column justify-content-between">
                            <div>
                                <a href="/" class="navbar-brand text-white">
                                    <img src="/images/uts-logo.svg" alt="UTS" class="me-2" style="height:36px;width:auto;">
                                    <span>Universidad Tecnológica de Santander</span>
                                </a>
                                <h2 class="mt-4 mb-2">Únete a la comunidad UTS</h2>
                                <p class="mb-3 opacity-75">Crea tu cuenta para acceder a herramientas de gestión académica</p>
                                <ul class="list-unstyled mt-3 mb-0">
                                    <li class="d-flex align-items-start gap-2 mb-2"><i class="bi bi-journal-text"></i><span>Registro de avances y documentos</span></li>
                                    <li class="d-flex align-items-start gap-2 mb-2"><i class="bi bi-chat-dots"></i><span>Asesorías y comunicación institucional</span></li>
                                    <li class="d-flex align-items-start gap-2"><i class="bi bi-shield-check"></i><span>Seguridad y trazabilidad de tus procesos</span></li>
                                </ul>
                            </div>
                            <div class="mt-4">
                                <small class="opacity-75">¿Ya tienes cuenta? <a href="/auth/login" class="text-white text-decoration-underline">Inicia sesión</a></small>
                            </div>
                        </div>

                        <!-- Formulario de registro -->
                        <div class="col-md-7 p-4 p-md-5">
                            <% if (error && error.length) { %>
                                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                                    <%= error[0] %>
                                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                                </div>
                            <% } %>
                            <% if (success && success.length) { %>
                                <div class="alert alert-success alert-dismissible fade show" role="alert">
                                    <%= success[0] %>
                                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                                </div>
                            <% } %>

                            <h3 class="mb-3">Crear cuenta</h3>
                            <p class="text-muted">Completa los siguientes datos para registrarte</p>

                            <form action="/auth/register" method="POST" novalidate>
                                <div class="row g-3">
                                    <div class="col-md-6">
                                        <label for="nombre" class="form-label">Nombre</label>
                                        <div class="input-group">
                                            <span class="input-group-text"><i class="bi bi-person"></i></span>
                                            <input type="text" class="form-control" id="nombre" name="nombre" placeholder="Tu nombre" required>
                                        </div>
                                    </div>
                                    <div class="col-md-6">
                                        <label for="apellido" class="form-label">Apellido</label>
                                        <div class="input-group">
                                            <span class="input-group-text"><i class="bi bi-person"></i></span>
                                            <input type="text" class="form-control" id="apellido" name="apellido" placeholder="Tu apellido" required>
                                        </div>
                                    </div>
                                    <div class="col-12">
                                        <label for="email" class="form-label">Correo Electrónico</label>
                                        <div class="input-group">
                                            <span class="input-group-text"><i class="bi bi-envelope"></i></span>
                                            <input type="email" class="form-control" id="email" name="email" placeholder="nombre@correo.com" required>
                                        </div>
                                        <div class="form-text">Usa tu correo institucional si dispones de él.</div>
                                    </div>
                                    <div class="col-12">
                                        <label for="password" class="form-label">Contraseña</label>
                                        <div class="input-group">
                                            <span class="input-group-text"><i class="bi bi-lock"></i></span>
                                            <input type="password" class="form-control" id="password" name="password" placeholder="••••••••" required>
                                            <button class="btn btn-outline-secondary" type="button" id="togglePassword" aria-label="Mostrar/Ocultar contraseña"><i class="bi bi-eye"></i></button>
                                        </div>
                                        <div class="form-text">Mínimo 8 caracteres, usa mayúsculas y números.</div>
                                    </div>
                                    <div class="col-12">
                                        <label for="rol_id" class="form-label">Rol</label>
                                        <div class="input-group">
                                            <span class="input-group-text"><i class="bi bi-badge-ad"></i></span>
                                            <select id="rol_id" name="rol_id" class="form-select" required>
                                                <option value="">Selecciona un rol</option>
                                                <% if (roles && roles.length) { %>
                                                    <% roles.forEach(function(r){ %>
                                                        <option value="<%= r.id %>"><%= r.nombre %></option>
                                                    <% }) %>
                                                <% } %>
                                            </select>
                                        </div>
                                    </div>
                                </div>
                                <div class="d-grid mt-4">
                                    <button type="submit" class="btn btn-primary py-2">Registrarse</button>
                                </div>
                                <p class="small text-muted mt-3">Al registrarte aceptas el código de conducta UTS y políticas de uso</p>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <%- include('../partials/scripts') %>
    <script>
        (function(){
            const toggleBtn = document.getElementById('togglePassword');
            const pwdInput = document.getElementById('password');
            if(toggleBtn && pwdInput){
                toggleBtn.addEventListener('click', function(){
                    const isText = pwdInput.type === 'text';
                    pwdInput.type = isText ? 'password' : 'text';
                    const icon = this.querySelector('i');
                    if(icon){
                        icon.classList.toggle('bi-eye');
                        icon.classList.toggle('bi-eye-slash');
                    }
                });
            }
        })();
    </script>
</body>
</html>