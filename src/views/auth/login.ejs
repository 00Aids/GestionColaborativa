<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'Iniciar Sesión' %></title>
    <%- include('../partials/head') %>
    <style>
        :root{--uts-primary:#667eea;--uts-secondary:#764ba2;--uts-gray-100:#f7f9fc;--uts-gradient:linear-gradient(135deg,#667eea 0%,#764ba2 100%);} 
        body{background-color: var(--uts-gray-100);}/* Fondo general gris institucional */
        .gradient-uts{background: var(--uts-gradient)!important;color:#fff;}/* Panel lateral verde institucional */
    </style>
</head>
<body class="min-vh-100 d-flex align-items-center">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-10 col-xl-8">
                <div class="card shadow-soft overflow-hidden">
                    <div class="row g-0">
                        <!-- Panel informativo académico -->
                        <div class="col-md-5 gradient-uts p-4 p-md-5 d-flex flex-column justify-content-between">
                            <div>
                                <a href="/" class="navbar-brand text-white">
                                    <img src="/images/uts-logo.svg" alt="UTS" class="me-2" style="height:36px;width:auto;">
                                    <span>Universidad Tecnológica de Santander</span>
                                </a>
                                <h2 class="mt-4 mb-2">Portal Académico</h2>
                                <p class="mb-3 opacity-75">Accede al Sistema de Gestión Académica y aprovecha las herramientas para tu progreso.</p>
                                <ul class="list-unstyled mt-3 mb-0">
                                    <li class="d-flex align-items-start gap-2 mb-2"><i class="bi bi-mortarboard-fill"></i><span>Gestión de proyectos y entregables</span></li>
                                    <li class="d-flex align-items-start gap-2 mb-2"><i class="bi bi-people-fill"></i><span>Interacción con coordinadores y directores</span></li>
                                    <li class="d-flex align-items-start gap-2"><i class="bi bi-graph-up"></i><span>Indicadores y progreso académico</span></li>
                                </ul>
                            </div>
                            <div class="mt-4">
                                <small class="opacity-75">© <%= new Date().getFullYear() %></small>
                            </div>
                        </div>

                        <!-- Formulario de acceso -->
                        <div class="col-md-7 p-4 p-md-5">
                            <% if (error && error.length) { %>
                                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                                    <%= error[0] %>
                                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                                </div>
                            <% } %>
                            <% if (success && success.length) { %>
                                <div class="alert alert-success alert-dismissible fade show" role="alert">
                                    <%= success[0] %>
                                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                                </div>
                            <% } %>

                            <h3 class="mb-3">Iniciar Sesión</h3>
                            <p class="text-muted mb-3">Ingresa con tu correo institucional y contraseña</p>
                            <form action="/auth/login" method="POST" novalidate>
                                <div class="input-group mb-3">
                                    <span class="input-group-text"><i class="bi bi-envelope"></i></span>
                                    <input type="email" class="form-control" id="email" name="email" placeholder="nombre@correo.com" required>
                                </div>
                                <div class="input-group mb-3">
                                    <span class="input-group-text"><i class="bi bi-lock"></i></span>
                                    <input type="password" class="form-control" id="password" name="password" placeholder="••••••••" required>
                                    <button class="btn btn-outline-secondary" type="button" id="togglePassword" aria-label="Mostrar/Ocultar contraseña"><i class="bi bi-eye"></i></button>
                                </div>
                                <div class="d-flex justify-content-between align-items-center mb-4">
                                    <div class="form-check">
                                        <input class="form-check-input" type="checkbox" id="remember" name="remember">
                                        <label class="form-check-label" for="remember">Recordar sesión</label>
                                    </div>
                                    <a href="#" class="text-muted small">¿Olvidaste la contraseña?</a>
                                </div>
                                <div class="d-grid gap-2 d-md-flex">
                                    <button type="submit" class="btn btn-primary px-4">Iniciar Sesión</button>
                                    <a href="/auth/register" class="btn btn-outline-primary px-4">Registrarse</a>
                                </div>
                                <p class="small text-muted mt-3">Al acceder aceptas el código de conducta institucional y políticas de uso</p>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <%- include('../partials/scripts') %>
    <script>
        (function(){
            const toggleBtn = document.getElementById('togglePassword');
            const pwdInput = document.getElementById('password');
            if(toggleBtn && pwdInput){
                toggleBtn.addEventListener('click', function(){
                    const isText = pwdInput.type === 'text';
                    pwdInput.type = isText ? 'password' : 'text';
                    const icon = this.querySelector('i');
                    if(icon){
                        icon.classList.toggle('bi-eye');
                        icon.classList.toggle('bi-eye-slash');
                    }
                });
            }
        })();
    </script>
</body>
</html>