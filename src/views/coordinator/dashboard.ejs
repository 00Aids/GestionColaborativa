<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - Coordinador</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            color: #2d3748;
            min-height: 100vh;
        }
        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 20px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        }
        .header-content {
            max-width: 1400px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .logo {
            font-size: 24px;
            font-weight: 700;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .user-info {
            display: flex;
            align-items: center;
            gap: 15px;
        }
        .area-badge {
            background: rgba(255,255,255,0.2);
            padding: 5px 12px;
            border-radius: 15px;
            font-size: 12px;
            backdrop-filter: blur(10px);
        }
        .role-badge {
            background: #ed8936;
            color: white;
            padding: 5px 15px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
        }
        .logout-btn {
            background: rgba(255,255,255,0.2);
            color: white;
            padding: 8px 16px;
            border: 1px solid rgba(255,255,255,0.3);
            border-radius: 6px;
            cursor: pointer;
            text-decoration: none;
            transition: all 0.3s;
            backdrop-filter: blur(10px);
        }
        .logout-btn:hover {
            background: rgba(255,255,255,0.3);
            transform: translateY(-1px);
        }
        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 30px 20px;
        }
        .dashboard-title {
            margin-bottom: 30px;
            text-align: center;
        }
        .dashboard-title h1 {
            font-size: 32px;
            color: #2d3748;
            margin-bottom: 8px;
            font-weight: 700;
        }
        .dashboard-title p {
            color: #718096;
            font-size: 16px;
        }
        .area-info {
            background: linear-gradient(135deg, #4299e1 0%, #3182ce 100%);
            color: white;
            padding: 15px 25px;
            border-radius: 12px;
            margin-bottom: 30px;
            text-align: center;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        }
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 25px;
            margin-bottom: 40px;
        }
        .stat-card {
            background: white;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.07);
            text-align: center;
            transition: all 0.3s;
            border-left: 4px solid transparent;
        }
        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 25px rgba(0,0,0,0.15);
        }
        .stat-card.projects { border-left-color: #4299e1; }
        .stat-card.deliverables { border-left-color: #ed8936; }
        .stat-card.evaluations { border-left-color: #48bb78; }
        .stat-card.students { border-left-color: #9f7aea; }
        .stat-icon {
            font-size: 40px;
            margin-bottom: 15px;
            opacity: 0.8;
        }
        .stat-number {
            font-size: 36px;
            font-weight: 700;
            color: #2d3748;
            margin-bottom: 8px;
        }
        .stat-label {
            color: #718096;
            font-size: 14px;
            font-weight: 500;
        }
        .stat-sublabel {
            color: #a0aec0;
            font-size: 12px;
            margin-top: 5px;
        }
        .main-grid {
            display: grid;
            grid-template-columns: 2fr 1fr;
            gap: 30px;
            margin-bottom: 30px;
        }
        .secondary-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 30px;
            margin-bottom: 30px;
        }
        .card {
            background: white;
            border-radius: 15px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.07);
            overflow: hidden;
            transition: all 0.3s;
        }
        .card:hover {
            box-shadow: 0 8px 25px rgba(0,0,0,0.15);
        }
        .card-header {
            padding: 20px 25px;
            border-bottom: 1px solid #e2e8f0;
            font-weight: 600;
            color: #2d3748;
            background: linear-gradient(135deg, #f7fafc 0%, #edf2f7 100%);
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .card-content {
            padding: 25px;
        }
        .list-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 0;
            border-bottom: 1px solid #f7fafc;
            transition: all 0.2s;
        }
        .list-item:hover {
            background: #f7fafc;
            margin: 0 -25px;
            padding: 15px 25px;
            border-radius: 8px;
        }
        .list-item:last-child {
            border-bottom: none;
        }
        .quick-actions {
            display: grid;
            gap: 12px;
        }
        .action-btn {
            background: linear-gradient(135deg, #ed8936 0%, #dd6b20 100%);
            color: white;
            padding: 15px;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            text-decoration: none;
            text-align: center;
            transition: all 0.3s;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 10px;
            justify-content: center;
        }
        .action-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(237, 137, 54, 0.3);
        }
        .status-badge {
            padding: 6px 12px;
            border-radius: 15px;
            font-size: 11px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        .status-activo, .status-en_desarrollo, .status-aprobado {
            background: #c6f6d5;
            color: #22543d;
        }
        .status-pendiente, .status-borrador, .status-enviado {
            background: #fef5e7;
            color: #744210;
        }
        .status-completado, .status-finalizado {
            background: #bee3f8;
            color: #2a4365;
        }
        .status-vencido, .status-rechazado {
            background: #fed7d7;
            color: #742a2a;
        }
        .activity-item {
            display: flex;
            align-items: center;
            gap: 15px;
            padding: 12px 0;
            border-bottom: 1px solid #f7fafc;
        }
        .activity-item:last-child {
            border-bottom: none;
        }
        .activity-icon {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 16px;
        }
        .activity-content {
            flex: 1;
        }
        .activity-title {
            font-weight: 600;
            color: #2d3748;
            font-size: 14px;
        }
        .activity-time {
            color: #718096;
            font-size: 12px;
        }
        .invitation-code {
            background: linear-gradient(135deg, #f7fafc 0%, #edf2f7 100%);
            border: 2px dashed #cbd5e0;
            border-radius: 10px;
            padding: 15px;
            margin-bottom: 15px;
            transition: all 0.3s;
        }
        .invitation-code:hover {
            border-color: #667eea;
            background: linear-gradient(135deg, #ebf8ff 0%, #bee3f8 100%);
        }
        .code-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 8px;
        }
        .code-value {
            font-family: 'Courier New', monospace;
            font-weight: 700;
            color: #2d3748;
            font-size: 16px;
        }
        .code-type {
            background: #667eea;
            color: white;
            padding: 4px 8px;
            border-radius: 12px;
            font-size: 11px;
            font-weight: 600;
        }
        .code-usage {
            color: #718096;
            font-size: 12px;
        }
        .notification-item {
            display: flex;
            align-items: flex-start;
            gap: 12px;
            padding: 12px 0;
            border-bottom: 1px solid #f7fafc;
        }
        .notification-item:last-child {
            border-bottom: none;
        }
        .notification-dot {
            width: 8px;
            height: 8px;
            border-radius: 50%;
            background: #ed8936;
            margin-top: 6px;
            flex-shrink: 0;
        }
        .notification-content {
            flex: 1;
        }
        .notification-title {
            font-weight: 600;
            color: #2d3748;
            font-size: 13px;
            margin-bottom: 2px;
        }
        .notification-time {
            color: #a0aec0;
            font-size: 11px;
        }
        .empty-state {
            text-align: center;
            color: #a0aec0;
            padding: 40px 20px;
        }
        .empty-state-icon {
            font-size: 48px;
            margin-bottom: 15px;
            opacity: 0.5;
        }
        @media (max-width: 768px) {
            .main-grid, .secondary-grid {
                grid-template-columns: 1fr;
            }
            .stats-grid {
                grid-template-columns: repeat(2, 1fr);
            }
            .container {
                padding: 20px 15px;
            }
        }
    </style>
</head>
<body>
    <!-- Header -->
    <div class="header">
        <div class="header-content">
            <div class="logo">
                🎯 Panel de Coordinación
            </div>
            <div class="user-info">
                <% if (areaInfo) { %>
                    <span class="area-badge">📍 <%= areaInfo.codigo %></span>
                <% } %>
                <span><%= user.nombres %> <%= user.apellidos %></span>
                <span class="role-badge">Coordinador</span>
                <a href="/auth/logout" class="logout-btn">Cerrar Sesión</a>
            </div>
        </div>
    </div>

    <!-- Main Content -->
    <div class="container">
        <div class="dashboard-title">
            <h1>Dashboard de Coordinación</h1>
            <p>Gestión integral de proyectos académicos y supervisión estudiantil</p>
        </div>

        <!-- Información del Área -->
        <% if (areaInfo) { %>
            <div class="area-info">
                <strong>🏢 Área de Trabajo: <%= areaInfo.nombre || areaInfo.codigo %></strong>
                <p style="margin-top: 5px; opacity: 0.9;">Coordinando proyectos y estudiantes en esta área específica</p>
            </div>
        <% } %>

        <!-- Estadísticas Principales -->
        <div class="stats-grid">
            <div class="stat-card projects">
                <div class="stat-icon">📁</div>
                <div class="stat-number"><%= projectStats?.total || 0 %></div>
                <div class="stat-label">Proyectos Totales</div>
                <div class="stat-sublabel">
                    <%= projectStats?.activos || 0 %> activos • <%= projectStats?.completados || 0 %> completados
                </div>
            </div>
            <div class="stat-card deliverables">
                <div class="stat-icon">📦</div>
                <div class="stat-number"><%= deliverableStats?.total || 0 %></div>
                <div class="stat-label">Entregables</div>
                <div class="stat-sublabel">
                    <%= deliverableStats?.pendientes || 0 %> pendientes • <%= deliverableStats?.vencidos || 0 %> vencidos
                </div>
            </div>
            <div class="stat-card evaluations">
                <div class="stat-icon">⭐</div>
                <div class="stat-number"><%= evaluationStats?.total || 0 %></div>
                <div class="stat-label">Evaluaciones</div>
                <div class="stat-sublabel">
                    Promedio: <%= evaluationStats?.promedio || 0 %> • <%= evaluationStats?.pendientes || 0 %> pendientes
                </div>
            </div>
            <div class="stat-card students">
                <div class="stat-icon">👥</div>
                <div class="stat-number"><%= students?.length || 0 %></div>
                <div class="stat-label">Estudiantes</div>
                <div class="stat-sublabel">Bajo supervisión en el área</div>
            </div>
        </div>

        <!-- Contenido Principal -->
        <div class="main-grid">
            <!-- Proyectos Bajo Supervisión -->
            <div class="card">
                <div class="card-header">
                    📁 Proyectos Bajo Mi Supervisión
                </div>
                <div class="card-content">
                    <% if (myProjects && myProjects.length > 0) { %>
                        <% myProjects.forEach(project => { %>
                            <div class="list-item">
                                <div>
                                    <strong><%= project.titulo %></strong><br>
                                    <small style="color: #718096;">
                                        👤 <%= project.estudiante_nombres %> <%= project.estudiante_apellidos %>
                                    </small>
                                </div>
                                <span class="status-badge status-<%= project.estado %>">
                                    <%= project.estado %>
                                </span>
                            </div>
                        <% }) %>
                    <% } else { %>
                        <div class="empty-state">
                            <div class="empty-state-icon">📁</div>
                            <p>No tienes proyectos asignados para supervisión</p>
                        </div>
                    <% } %>
                </div>
            </div>

            <!-- Acciones Rápidas -->
            <div class="card">
                <div class="card-header">
                    🚀 Acciones Rápidas
                </div>
                <div class="card-content">
                    <div class="quick-actions">
                        <a href="/coordinator/projects" class="action-btn">
                            📁 Gestionar Proyectos
                        </a>
                        <a href="/coordinator/students" class="action-btn">
                            👥 Mis Estudiantes
                        </a>
                        <a href="/coordinator/evaluations" class="action-btn">
                            ⭐ Evaluaciones
                        </a>
                        <a href="/coordinator/reports" class="action-btn">
                            📊 Reportes
                        </a>
                        <a href="/coordinator/calendar" class="action-btn">
                            📅 Calendario
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <!-- Contenido Secundario -->
        <div class="secondary-grid">
            <!-- Actividad Reciente -->
            <div class="card">
                <div class="card-header">
                    🕒 Actividad Reciente
                </div>
                <div class="card-content">
                    <% if (recentActivity && recentActivity.length > 0) { %>
                        <% recentActivity.forEach(activity => { %>
                            <div class="activity-item">
                                <div class="activity-icon"><%= activity.icono %></div>
                                <div class="activity-content">
                                    <div class="activity-title"><%= activity.titulo %></div>
                                    <div class="activity-time">
                                        <%= new Date(activity.fecha).toLocaleDateString('es-ES', { 
                                            day: 'numeric', 
                                            month: 'short', 
                                            hour: '2-digit', 
                                            minute: '2-digit' 
                                        }) %>
                                    </div>
                                </div>
                            </div>
                        <% }) %>
                    <% } else { %>
                        <div class="empty-state">
                            <div class="empty-state-icon">🕒</div>
                            <p>No hay actividad reciente</p>
                        </div>
                    <% } %>
                </div>
            </div>

            <!-- Códigos de Invitación -->
            <div class="card">
                <div class="card-header">
                    🎫 Códigos de Invitación
                </div>
                <div class="card-content">
                    <% if (invitationCodes && invitationCodes.length > 0) { %>
                        <% invitationCodes.forEach(code => { %>
                            <div class="invitation-code">
                                <div class="code-header">
                                    <span class="code-value"><%= code.codigo %></span>
                                    <span class="code-type"><%= code.tipo %></span>
                                </div>
                                <div class="code-usage">
                                    <%= code.usos %>/<%= code.limite %> usos • Expira: <%= code.expira %>
                                </div>
                            </div>
                        <% }) %>
                    <% } else { %>
                        <div class="empty-state">
                            <div class="empty-state-icon">🎫</div>
                            <p>No hay códigos activos</p>
                        </div>
                    <% } %>
                </div>
            </div>
        </div>

        <!-- Entregables y Notificaciones -->
        <div class="secondary-grid">
            <!-- Entregables Próximos a Vencer -->
            <div class="card">
                <div class="card-header">
                    ⏰ Entregables Próximos a Vencer
                </div>
                <div class="card-content">
                    <% if (upcomingDeliverables && upcomingDeliverables.length > 0) { %>
                        <% upcomingDeliverables.forEach(deliverable => { %>
                            <div class="list-item">
                                <div>
                                    <strong><%= deliverable.titulo %></strong><br>
                                    <small style="color: #718096;">
                                        📁 <%= deliverable.proyecto_titulo %>
                                    </small>
                                </div>
                                <span style="color: #ed8936; font-size: 12px; font-weight: 600;">
                                    <%= new Date(deliverable.fecha_limite).toLocaleDateString('es-ES', { 
                                        day: 'numeric', 
                                        month: 'short' 
                                    }) %>
                                </span>
                            </div>
                        <% }) %>
                    <% } else { %>
                        <div class="empty-state">
                            <div class="empty-state-icon">⏰</div>
                            <p>No hay entregables próximos a vencer</p>
                        </div>
                    <% } %>
                </div>
            </div>

            <!-- Notificaciones -->
            <div class="card">
                <div class="card-header">
                    🔔 Notificaciones Recientes
                </div>
                <div class="card-content">
                    <% if (notifications && notifications.length > 0) { %>
                        <% notifications.forEach(notification => { %>
                            <div class="notification-item">
                                <div class="notification-dot"></div>
                                <div class="notification-content">
                                    <div class="notification-title"><%= notification.titulo %></div>
                                    <div class="notification-time">
                                        <%= new Date(notification.created_at).toLocaleDateString('es-ES', { 
                                            day: 'numeric', 
                                            month: 'short', 
                                            hour: '2-digit', 
                                            minute: '2-digit' 
                                        }) %>
                                    </div>
                                </div>
                            </div>
                        <% }) %>
                    <% } else { %>
                        <div class="empty-state">
                            <div class="empty-state-icon">🔔</div>
                            <p>No hay notificaciones</p>
                        </div>
                    <% } %>
                </div>
            </div>
        </div>
    </div>

    <!-- Mensajes Flash -->
    <% if (success && success.length > 0) { %>
        <div style="position: fixed; top: 20px; right: 20px; background: linear-gradient(135deg, #c6f6d5 0%, #9ae6b4 100%); color: #22543d; padding: 15px 20px; border-radius: 10px; box-shadow: 0 4px 6px rgba(0,0,0,0.1); z-index: 1000; border-left: 4px solid #48bb78;">
            <strong>✅ Éxito:</strong> <%= success %>
        </div>
    <% } %>
    <% if (error && error.length > 0) { %>
        <div style="position: fixed; top: 20px; right: 20px; background: linear-gradient(135deg, #fed7d7 0%, #feb2b2 100%); color: #742a2a; padding: 15px 20px; border-radius: 10px; box-shadow: 0 4px 6px rgba(0,0,0,0.1); z-index: 1000; border-left: 4px solid #f56565;">
            <strong>❌ Error:</strong> <%= error %>
        </div>
    <% } %>

    <script>
        // Auto-hide flash messages
        setTimeout(() => {
            const flashMessages = document.querySelectorAll('[style*="position: fixed"]');
            flashMessages.forEach(msg => {
                if (msg.textContent.includes('Éxito:') || msg.textContent.includes('Error:')) {
                    msg.style.opacity = '0';
                    msg.style.transform = 'translateX(100%)';
                    setTimeout(() => msg.remove(), 300);
                }
            });
        }, 5000);

        // Add hover effects to cards
        document.querySelectorAll('.stat-card').forEach(card => {
            card.addEventListener('mouseenter', () => {
                card.style.transform = 'translateY(-5px) scale(1.02)';
            });
            card.addEventListener('mouseleave', () => {
                card.style.transform = 'translateY(0) scale(1)';
            });
        });
    </script>
</body>
</html>